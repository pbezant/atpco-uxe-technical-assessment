/**
 * Each member is a color style. Use for setting element color style.
 * @readonly
 * @property {string} VisualColor.default - empty string
 * @property {string} VisualColor.primary
 * @property {string} VisualColor.secondary
 * @property {string} VisualColor.accent
 * @property {string} VisualColor.success
 * @property {string} VisualColor.warning
 * @property {string} VisualColor.danger
 * @property {string} VisualColor.weak
 * @property {string} VisualColor.strong
 * @property {string} VisualColor.inverse
 * @property {string} VisualColor.tertiary
 * @property {string} VisualColor.disabled
 * @property {string} VisualColor.utility
 * */
export declare enum VisualColor {
    DEFAULT = "",
    PRIMARY = "primary",
    SECONDARY = "secondary",
    ACCENT = "accent",
    SUCCESS = "success",
    WARNING = "warning",
    DANGER = "danger",
    WEAK = "weak",
    STRONG = "strong",
    INVERSE = "inverse",
    TERTIARY = "tertiary",
    DISABLED = "disabled",
    UTILITY = "utility"
}
/**
 * Each member is a size style. Use for setting element size style.
 * @readonly
 * @property {string} VisualSize.large
 * @property {string} VisualSize.medium
 * @property {string} VisualSize.small
 * */
export declare enum VisualSize {
    LARGE = "large",
    MEDIUM = "medium",
    SMALL = "small"
}
export declare enum SegmentSize {
    LARGE = "large",
    MEDIUM = "medium",
    SMALL = "small"
}
export declare enum SegmentColor {
    DEFAULT = "default",
    UTILITY_BLUE = "utility-blue",
    SLATE = "slate"
}
/**
 * Each member is a position.
 * @readonly
 * @property {string} VisualPosition.left
 * @property {string} VisualPosition.right
 * @property {string} VisualPosition.top
 * @property {string} VisualPosition.bottom
 * */
export declare enum VisualPosition {
    LEFT = "left",
    RIGHT = "right",
    TOP = "top",
    BOTTOM = "bottom"
}
